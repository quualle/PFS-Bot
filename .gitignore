# Ignore everything
*
*/

# Allow Python and HTML files everywhere
!*.py
!*.html

# Allow JSON files ONLY in the root directory
/*.json

# Allow directories to be tracked so we can recurse into them
!*/

# Ignore specific files even if they match the above extensions
.DS_Store
__pycache__/
*.pyc
*.pyo
*.pyd
*~
.vscode/
.idea/
